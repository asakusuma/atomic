/*global ATOMIC_CONFIG:true */

/**
 * Atomic configuration
 * important URL stuff would go here
 */
var ATOMIC_CONFIG = {
  system: 'inject',
  inject: {
    atomicjs: 'http://localhost:4321/atomic.js',
    modules: 'http://localhost:4321/atomic/',
    resolver: null,
    xdHtml: null,
    xdSwf: null
  }
};

ATOMIC_CONFIG = ATOMIC_CONFIG;